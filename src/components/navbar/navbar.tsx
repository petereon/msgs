import React from 'react';
import './navbar.scss';

const Navbar: React.FC = () => {
    return (
        <div className='navbar'>
            {/* Navbar content */}
        </div>
    );
};

export default Navbar;
